{
  "name": "newmath",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "postinstall": "mkdir -p public/wasm && cp node_modules/minizinc/dist/minizinc-worker.js node_modules/minizinc/dist/minizinc.wasm node_modules/minizinc/dist/minizinc.data public/wasm/"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "minizinc": "^4.4.5",
    "zod": "^4.3.6"
  }
}
